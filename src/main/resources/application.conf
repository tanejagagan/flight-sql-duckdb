flight-sql-duckdb = {
    port = 55555
    host = "localhost"
    keystore = "security/server0.key"
    serverCert = "security/server0.pem"
    users = [{
        name = admin
        password = admin
    }]
}